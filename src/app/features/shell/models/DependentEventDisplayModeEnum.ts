export enum DependentEventDisplayModeEnum {
    // show as timer event
    TimerEvent = 0,
    // show below timer event slot
    BelowTimerEvent = 1,
    // show below all timer event slots
    BelowTimerEvents = 2
}